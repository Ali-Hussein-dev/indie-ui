---
title: Separator
description: basic separators
---

import {ComponentPreview} from '@/components/component-preview';
import { Separator } from '@/components/separators/separator';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { FaPlus } from 'react-icons/fa6';

## Separator
<ComponentPreview filePathsKey="Separator">

<div className="flex-col-center gap-12 pt-6">
    <Separator gradient />
    <Separator />
    <Separator label={<span className='px-2'>Section</span>} gradient />
    <Separator label={<span className='px-2'>Section</span>} />
    <Separator label={<div className='border px-4 py-1 rounded-full border-dashed'>Section</div>} gradient />
    <Separator label={<div className='border px-4 py-1 rounded-full'>Section</div>} />
    <Separator label={<div className='border px-12 py-2 rounded-full'>
    <FaPlus />
    </div>} gradient />
  </div>

</ComponentPreview>

## Props

<TypeTable
  type={{
    label: {
      type: 'React.ReactNode',
      description: 'Label for the separator',
      default: 'empty string',
    },
    orientation: {
      type: "'horizontal' | 'vertical'",
      description: 'Orientation of the separator',
      default: "'horizontal'",
    },
    gradient: {
      type: 'boolean',
      description: 'Apply gradient to the separator',
      default: 'false',
    },
    className: {
      type: 'string',
      description: 'Additional classes to be applied to the separator',
      default: 'empty string',
    },
  }}
/>
