---
  title: Shadcn Button
  description: Extended shadcn button component.
---

import { Button } from '@/components/buttons/extended-shadcn-button';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { IoSend, IoArrowBack } from 'react-icons/io5';
import { LuMoon } from 'react-icons/lu';
import { LuArrowRight, LuArrowLeft } from 'react-icons/lu';
import { ComponentPreview } from '@/components/component-preview';

## Extended shadcn button

This is a modified version of shadcn button component. It takes rightIcon and leftIcon as props to add icons to the button.


## Variants Props 

<ComponentPreview filePathsKey="extended_shadcn_button" className="flex-row-start gap-3 flex-wrap">
  <Button>Default</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="destructive">Destructive</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="link">Link</Button>

</ComponentPreview>

## Side icons (extended props)

<div className="flex-row-start gap-3 flex-wrap">
  <Button rightIcon={<LuArrowRight />} variant="outline">
    <span>With right icon</span>
  </Button>
  <Button leftIcon={<LuArrowLeft />} variant="outline">
    <span>With left icon</span>
  </Button>
</div>

## Size Prop

Use the `size` prop to create different button sizes

<div className="flex-row-start gap-3 flex-wrap">
  <Button size="sm">Small</Button>
  <Button>Default</Button>
  <Button size="lg">Large</Button>
</div>

## Icon Button

Use the `size="icon"` prop to create an icon button

<div className="flex-row-start gap-3 flex-wrap">
  <Button size="icon">
    <LuMoon />
  </Button>
  <Button size="icon" variant="secondary">
    <LuMoon />
  </Button>
  <Button size="icon" variant="outline">
    <LuMoon />
  </Button>
  <Button size="icon" variant="ghost">
    <LuMoon />
  </Button>
  <Button size="icon" variant="destructive">
    <LuMoon />
  </Button>
</div>

