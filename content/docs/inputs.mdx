---
title: Input
description: Input fields with variant styles.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { InputPreview } from '@/components/inputs/input-preview';

## Input block
The InputBlock component is a customizable wrapper for input element, allowing you to add sections on the left and right sides of the input.

<InputPreview />

### InputBlock Props

<TypeTable
  type={{
    variant: {
      type: 'default|filled|underlined|ghost|neubrutalism',
      description: 'Change the input style',
      default: 'default',
    },
    size: {
      type: 'sm|default|lg',
      description: 'Change the size of the input block',
      default: 'default',
    },
    leftSection: {
      type: 'React.ReactNode',
      description: 'Use for adding icon on the left side of the input',
      default: 'undefined',
    },
    rightSection: {
      type: 'React.ReactNode',
      description: 'Use for adding icon on the right side of the input',
      default: 'undefined',
    },
  }}
/>
