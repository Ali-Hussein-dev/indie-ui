---
title: Input block
description: Input block variants
---

import { Tab, Tabs } from "fumadocs-ui/components/tabs"
import { IoSearchSharp } from "react-icons/io5"
import { BiSolidSend } from "react-icons/bi"
import { Input, InputBlock } from "@/src/components/inputs/inputs"
import { TypeTable } from "fumadocs-ui/components/type-table"

<a href="#installation-guide">Installation Guide</a>

## Default (outline)

<Tabs items={["Preview"]}>
  <Tab value="Preview">
    <div className="py-4 mx-auto max-w-xl space-y-3">
      <InputBlock
        input={{ placeholder: "Input Small" }}
        root={{ size: "sm" }}
      />
      <InputBlock input={{ placeholder: "Input default" }} />
      <InputBlock
        input={{ placeholder: "Input Large" }}
        root={{ size: "lg" }}
      />
      <br />
      <InputBlock
        input={{ placeholder: "Input with left icon" }}
        leftIcon={<IoSearchSharp className="text-zinc-500" />}
      />
      <InputBlock
        input={{ placeholder: "Input with right icon" }}
        rightIcon={<IoSearchSharp className="text-zinc-500" />}
      />
      <InputBlock
        input={{ placeholder: "Input with right & left icons" }}
        leftIcon={<IoSearchSharp className="text-zinc-500" />}
        rightIcon={<BiSolidSend className="text-zinc-600" />}
      />
    </div>
  </Tab>
</Tabs>

## Neubrutalism

<Tabs items={["Preview"]}>
  <Tab value="Preview">
    <div className="py-4 mx-auto max-w-xl space-y-3">
        <InputBlock
          input={{ placeholder: "Input Small" }}
          root={{variant:"neubrutalism", size:"sm" }}
          />
        <InputBlock
          input={{ placeholder: "Input default" }}
          root={{variant:"neubrutalism",  }}
          />
        <InputBlock
          input={{ placeholder: "Input Large" }}
          root={{variant:"neubrutalism", size: "lg" }}
          />
        <br/>
        <InputBlock
          input={{ placeholder: "Input with left icon" }}
          root={{variant:"neubrutalism",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right icon" }}
          root={{variant:"neubrutalism",}}
          rightIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right & left icons" }}
          root={{variant:"neubrutalism",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          rightIcon={<BiSolidSend className="text-zinc-600" />}
          />
    </div>
  </Tab>
</Tabs>

## Filled

<Tabs items={["Preview"]}>
  <Tab value="Preview">
    <div className="py-4 mx-auto max-w-xl space-y-3">
        <InputBlock
          input={{ placeholder: "Input Small" }}
          root={{variant:"filled", size:"sm" }}
          />
        <InputBlock
          input={{ placeholder: "Input default" }}
          root={{variant:"filled",  }}
          />
        <InputBlock
          input={{ placeholder: "Input Large" }}
          root={{variant:"filled", size: "lg" }}
          />
        <br/>
        <InputBlock
          input={{ placeholder: "Input with left icon" }}
          root={{variant:"filled",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right icon" }}
          root={{variant:"filled",}}
          rightIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right & left icons" }}
          root={{variant:"filled",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          rightIcon={<BiSolidSend className="text-zinc-600" />}
          />
    </div>
  </Tab>
</Tabs>

## Underlined

<Tabs items={["Preview"]}>
  <Tab value="Preview">
    <div className="py-4 mx-auto max-w-xl space-y-3">
        <InputBlock
          input={{ placeholder: "Input Small" }}
          root={{variant:"underlined", size:"sm" }}
          />
        <InputBlock
          input={{ placeholder: "Input default" }}
          root={{variant:"underlined",  }}
          />
        <InputBlock
          input={{ placeholder: "Input Large" }}
          root={{variant:"underlined", size: "lg" }}
          />
        <br/>
        <InputBlock
          input={{ placeholder: "Input with left icon" }}
          root={{variant:"underlined",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right icon" }}
          root={{variant:"underlined",}}
          rightIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right & left icons" }}
          root={{variant:"underlined",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          rightIcon={<BiSolidSend className="text-zinc-600" />}
          />
    </div>
  </Tab>
</Tabs>

## Ghost

<Tabs items={["Preview"]}>

  <Tab value="Preview">
    <div className="py-4 mx-auto max-w-xl space-y-3">
        <InputBlock
          input={{ placeholder: "Input Small" }}
          root={{variant:"ghost", size:"sm" }}
          />
        <InputBlock
          input={{ placeholder: "Input default" }}
          root={{variant:"ghost",  }}
          />
        <InputBlock
          input={{ placeholder: "Input Large" }}
          root={{variant:"ghost", size: "lg" }}
          />
        <br/>
        <InputBlock
          input={{ placeholder: "Input with left icon" }}
          root={{variant:"ghost",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right icon" }}
          root={{variant:"ghost",}}
          rightIcon={<IoSearchSharp className="text-zinc-500" />}
          />
        <InputBlock
          input={{ placeholder: "Input with right & left icons" }}
          root={{variant:"ghost",}}
          leftIcon={<IoSearchSharp className="text-zinc-500" />}
          rightIcon={<BiSolidSend className="text-zinc-600" />}
          />
    </div>
  </Tab>
  <Tab value="React">
</Tab>
</Tabs>

{/* ## Variant 5 - floating label */}

## Installation guide

If you don't have [shadcn](https://ui.shadcn.com/docs/installation) installed in your project, you need to install the following packages:

```bash title="Terminal"
pnpm add class-variance-authority clsx tailwind-merge
```

```bash title="cn.tsx"
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs));
}
```

If you have shadcn setup in your project, you can copy the code below and paste it in your project.

<Tabs items={["React"]}>
  <Tab value="React">
```tsx title="Usage.tsx"
export const Form = () => (
  <InputBlock
    root={{ variant: "filled", size: "lg" }}
    input={{ type: "text", placeholder: "Search" }}
    leftIcon={<SearchIcon />}
    rightIcon={<CloseIcon />}
  />
)
```

```tsx title="Input.tsx"
import * as React from "react"
import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "@/src/utils/cn"

export interface InputProps
  extends React.InputHTMLAttributes<HTMLInputElement> {}

const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          "size-full bg-transparent ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium dark:placeholder:text-zinc-600 placeholder:text-zinc-400 disabled:cursor-not-allowed disabled:opacity-50 focus-visible:outline-none text-base",
          className
        )}
        ref={ref}
        {...props}
      />
    )
  }
)

const rootVariants = cva(
  "py-1 px-2 flex rounded gap-1 items-center transition duration-200",
  {
    variants: {
      variant: {
        // outline
        default:
          "border ring-zinc-800 ring-zinc-300 focus-within:ring-2 focus-within:ring-offset-2 dark:focus-within:ring-zinc-400 focus-within:ring-offset-white dark:focus-within:ring-offset-black focus-within:ring-zinc-600",

        underlined:
          "border-b-[1px] dark:border-zinc-800 focus-within:border-b-2 dark:focus-within:border-zinc-400 focus-within:border-zinc-500 rounded-none px-0",

        filled:
          "bg-zinc-100 dark:bg-zinc-900 dark:text-zinc-100 focus-within:bg-zinc-200 dark:focus-within:bg-zinc-800",

        ghost:
          "bg-transparent dark:bg-transparent dark:text-zinc-100 focus-within:bg-zinc-100 dark:focus-within:bg-zinc-900",
        neubrutalism:
          "border border-zinc-700 rounded-sm shadow-[2px_2px_0px_rgb(82,82,91)] dark:shadow-[2px_2px_0px_rgb(82,82,91)] focus-within:bg-yellow-50 dark:focus-within:bg-zinc-900/40",
        // with floating label
      },
      size: {
        sm: "h-8",
        default: "h-10",
        lg: "h-12",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)

type InputBlockProps = {
  root: VariantProps<typeof rootVariants> & {
    className?: string
  }
  input: InputProps
  leftIcon?: React.ReactNode
  rightIcon?: React.ReactNode
}

const InputBlock = ({
  root: { size, variant, className } = { size: "default", variant: "default" },
  input,
  leftIcon,
  rightIcon,
}: InputBlockProps) => (
  <div className={cn(rootVariants({ variant, size, className }))}>
    {leftIcon && <span className="px-1">{leftIcon}</span>}
    <Input {...input} />
    {rightIcon && <span className="px-1">{rightIcon}</span>}
  </div>
)

export { Input, InputBlock }

Input.displayName = "Input"
InputBlock.displayName = "InputBlock"
```

</Tab>
</Tabs>


### Input Props
<TypeTable type={{
  variant: {
    type: "default|filled|underlined|ghost|neubrutalism",
    description: "change the input style",
    default: "default"
  },
  size: {
    type: "sm|default|lg",
    description: "Change the size of the input block",
    default: "default"
        },
  leftIcon: {
    type: "React.ReactNode",
    description: "Left icon",
    default: "undefined"
  },
  rightIcon: {
    type: "React.ReactNode",
    description: "Right icon",
    default: "undefined"
  },
    }} />
